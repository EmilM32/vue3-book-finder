export enum EChangePage {
  NEXT,
  PREVIOUS,
}

export enum EModules {
  BOOKS = 'books',
}

export enum EPages {
  HOME = 'Home',
  RESULTS = 'Results',
  DETAILS = 'Details',
  NOT_FOUND = 'NotFound',
}
